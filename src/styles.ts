export const theme = {
  palette: {
    primary: {
      main: 'rgb(var(--main-color))',
      light: '#62727b',
      dark: '#102027',
      contrastText: 'rgb(var(--text-color))',
    },
  },
}

export type Theme = typeof theme
